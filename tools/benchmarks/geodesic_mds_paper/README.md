# Geodesic MDS Benchmark Scripts

This directory contains benchmark drivers for the `geodesic_mds_paper` track.

These scripts generate the raw experiment outputs that are later summarized by the report renderers under `tools/reports/geodesic_mds_paper/`.
