# Geodesic MDS Report Scripts

This directory contains report and gallery renderers for the `geodesic_mds_paper` track.

These scripts typically consume benchmark artifacts from `tools/benchmarks/geodesic_mds_paper/` runs and produce interactive HTML or related report assets.
