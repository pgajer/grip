% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grip_quality.R
\name{grip.optimize.geodesic.mds}
\alias{grip.optimize.geodesic.mds}
\title{Optimize a layout under the geodesic-MDS objective}
\usage{
grip.optimize.geodesic.mds(
  coords = NULL,
  prepared = NULL,
  data = NULL,
  k = NULL,
  dim = 2L,
  connect = c("mst", "error"),
  init = c("cmdscale", "random", "user"),
  engine = c("cpp", "r"),
  max_iter = 16L,
  edge_length_epsilon = 1e-08,
  initial_step = 1,
  step_shrink = 0.5,
  armijo_factor = 1e-04,
  grad_tol = 1e-08,
  min_step = 1e-08,
  recenter = TRUE,
  return_trace = FALSE,
  seed = NULL
)
}
\arguments{
\item{coords}{Optional numeric coordinate matrix with 2 or 3 columns. If
omitted, coordinates are initialized according to \code{init}.}

\item{prepared}{Optional prepared object from
\code{\link{grip.prepare.geodesic.mds}()} or
\code{\link{grip.prepare.geodesic.kk}()}.}

\item{data}{Optional input data matrix used when \code{prepared} is omitted.}

\item{k}{Optional \eqn{k}-NN neighborhood size used when \code{prepared} is
omitted.}

\item{dim}{Target output dimension used when \code{coords} is omitted.}

\item{connect}{Connectivity policy used when \code{prepared} is omitted.}

\item{init}{Initialization mode: \code{"cmdscale"}, \code{"random"}, or
\code{"user"}.}

\item{engine}{Optimization engine: \code{"cpp"} or \code{"r"}.}

\item{max_iter}{Maximum number of gradient-descent iterations.}

\item{edge_length_epsilon}{Small non-negative stabilizer added inside each
embedded edge length.}

\item{initial_step}{Initial line-search step size.}

\item{step_shrink}{Multiplicative backtracking shrink factor in `(0, 1)`.}

\item{armijo_factor}{Non-negative Armijo decrease constant.}

\item{grad_tol}{Non-negative stopping tolerance on the gradient norm.}

\item{min_step}{Positive minimum accepted line-search step.}

\item{recenter}{If \code{TRUE}, recenter accepted proposals to zero mean.}

\item{return_trace}{If \code{TRUE}, include per-iteration diagnostics and
accepted frames.}

\item{seed}{Optional integer seed used only for random initialization.}
}
\value{
A list with \code{coords}, \code{trace}, \code{frames},
  \code{prepared}, and \code{score}.
}
\description{
\code{grip.optimize.geodesic.mds()} optimizes a fixed-path geodesic-MDS
objective using deterministic gradient descent with Armijo backtracking. By
default it initializes from classical MDS on the graph geodesic distance
matrix and then runs a compiled optimizer.
}
