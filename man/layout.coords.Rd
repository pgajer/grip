% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout_coords.R
\name{layout.coords}
\alias{layout.coords}
\title{Extract coordinates from a layout result}
\usage{
layout.coords(x)
}
\arguments{
\item{x}{A numeric coordinate matrix, a \code{grip_layout_trace}, a
\code{grip_gmds_layout}, or a refinement list with the documented
\code{coords/trace/frames/prepared/score} or \code{coords/state/trace/frames} structure.}
}
\value{
The coordinate matrix, unchanged, including vertex order, dimensions,
dimnames, and attributes. No projection, alignment, scaling, or scoring is
performed. Malformed, ambiguous, or unsupported objects produce an error.
}
\description{
Use one extraction step for ordinary layouts, traces, MDS fits, and supported
refinement results. Existing return types and direct field access are unchanged.
}
\details{
For traces the final frame is selected. For MDS and refinement fits
the fitted \code{coords} are selected. A plain list containing only \code{coords} is
not a recognized result; pass that matrix explicitly instead.

Keep graph vertex order when reusing coordinates. MDS result fields \code{method},
\code{coords}, \code{diagnostics}, and \code{metadata} remain directly available. Stopping
information in \code{metadata} is method-dependent. Trace \verb{$final} and refinement
\verb{$coords} remain supported. Prepared caches and internal state fields are
version-dependent: rebuild them when the graph or package contract changes.
}
\examples{
edges <- edges.path(6)
fits <- list(grip(edges, n = 6, dim = 2, seed = 1),
             trace.grip(edges, n = 6, dim = 2, seed = 1),
             classical.mds(edges = edges, n = 6))
first <- layout.coords(fits[[1]])
coords <- lapply(fits, layout.coords)
plot.layout(coords[[1]], edges = edges)
vapply(coords, function(z) score.layout(z, edges = edges, n = 6,
    sample.size.stress = 15, stress.seed = 1,
    edge.crossings = "never")$sampled.stress, numeric(1))
}
\seealso{
\code{\link[=grip]{grip()}}, \code{\link[=trace.grip]{trace.grip()}}, \code{\link[=classical.mds]{classical.mds()}}, \code{\link[=geodesic.kk]{geodesic.kk()}}
}
