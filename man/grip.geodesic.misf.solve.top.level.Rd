% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grip_geodesic_misf.R
\name{grip.geodesic.misf.solve.top.level}
\alias{grip.geodesic.misf.solve.top.level}
\title{Solve the coarsest MISF level with restartable pure GMDS}
\usage{
grip.geodesic.misf.solve.top.level(
  prepared,
  dim = 2L,
  n_restarts = 8L,
  max_iter = 16L,
  engine = c("cpp", "r"),
  edge_length_epsilon = 1e-08,
  initial_step = 1,
  step_shrink = 0.5,
  armijo_factor = 1e-04,
  grad_tol = 1e-08,
  min_step = 1e-08,
  n_threads = 0L,
  recenter = TRUE,
  return_trace = FALSE,
  seed = 6L
)
}
\arguments{
\item{prepared}{A MISF-GMDS prepared object from
`grip.prepare.misf.geodesic.mds()`, or directly a graph-first GMDS
prepared object representing a coarse level.}

\item{dim}{Target embedding dimension (`2` or `3`).}

\item{n_restarts}{Number of random restarts.}

\item{max_iter}{Maximum number of pure-GMDS iterations per restart.}

\item{engine}{Optimization engine passed through to
`grip.optimize.geodesic.mds()`.}

\item{edge_length_epsilon}{Small non-negative edge-length stabilizer.}

\item{initial_step}{Initial Armijo line-search step.}

\item{step_shrink}{Backtracking shrink factor.}

\item{armijo_factor}{Armijo decrease constant.}

\item{grad_tol}{Gradient-norm stopping tolerance.}

\item{min_step}{Minimum accepted line-search step.}

\item{n_threads}{Number of compiled-engine threads.}

\item{recenter}{Whether to recenter accepted proposals to zero mean.}

\item{return_trace}{Whether to retain per-iteration traces/frames for the
best restart.}

\item{seed}{Optional base seed; restart `r` uses `seed + r - 1`.}
}
\value{
The best restart fit, together with a restart summary and the coarse
  vertex ids.
}
\description{
`grip.geodesic.misf.solve.top.level()` runs a pure-GMDS solve on the coarse
top MISF graph using multiple random restarts. It is the Phase 2 replacement
for ad hoc random or spectral top-level initialization.
}
