% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_helpers.R
\name{irregular_shell_solid_helpers}
\alias{irregular_shell_solid_helpers}
\alias{irregular.shell.solid.graph}
\title{Weighted irregular shell solid helpers}
\usage{
irregular.shell.solid.graph(
  base = c("tetrahedron", "octahedron", "icosahedron"),
  level = 1,
  layers = 3,
  inner_radius = 0.45,
  outer_radius = 1,
  radial_irregularity = 0.25,
  layer_twist = 0.35,
  surface = c("standard", "bulged", "twisted", "wavy"),
  amplitude = 0.2,
  freq_theta = 2,
  freq_phi = 2,
  twist = 0.6,
  normalize = c("median", "mean", "none")
)
}
\arguments{
\item{base}{Base polyhedron. One of \code{"tetrahedron"},
\code{"octahedron"}, or \code{"icosahedron"}.}

\item{level}{Surface subdivision depth used for each radial layer.}

\item{layers}{Number of non-center radial layers.}

\item{inner_radius}{Positive inner radius of the shell.}

\item{outer_radius}{Positive outer radius of the ball.}

\item{radial_irregularity}{Irregularity level for the radial layer spacing.
Must lie in \code{[0, 1]}.}

\item{layer_twist}{Finite z-axis twist applied smoothly across radial
layers.}

\item{surface}{Solid geometry family. One of \code{"standard"},
\code{"bulged"}, \code{"twisted"}, or \code{"wavy"}.}

\item{amplitude}{Finite deformation amplitude.}

\item{freq_theta}{Positive azimuthal modulation frequency used only when
\code{surface = "wavy"}.}

\item{freq_phi}{Positive polar modulation frequency used only when
\code{surface = "wavy"}.}

\item{twist}{Finite twist strength used only when
\code{surface = "twisted"}.}

\item{normalize}{Normalization applied to the induced edge lengths. One of
\code{"median"}, \code{"mean"}, or \code{"none"}.}
}
\value{
The \code{coords_surface} component is an \code{n x 3} numeric
matrix with columns \code{x}, \code{y}, and \code{z}.

\code{irregular.shell.solid.graph()} returns the same components as
\code{irregular.ball.solid.graph()}, with \code{family} set to
\code{"irregular.shell"}.
}
\description{
Convenience helpers that build a layered simplicial shell by placing the
vertices of a subdivided triangulated polyhedron on nested inner-to-outer
radial layers and connecting adjacent layers by the same deterministic
prism-to-tetrahedra edge pattern used for \code{irregular.ball.solid.*()}.
The result is a genuinely volumetric shell graph with a hollow interior.
}
