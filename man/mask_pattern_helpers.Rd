% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_helpers.R
\name{mask_pattern_helpers}
\alias{mask_pattern_helpers}
\alias{mask.cross}
\alias{mask.border}
\alias{mask.corner}
\alias{mask.asymmetric.holes}
\title{Mask pattern helpers for recursive grid families}
\usage{
mask.cross(k = 5, arm_width = 1)

mask.border(k = 5, thickness = 1)

mask.corner(
  k = 5,
  width = 2,
  corner = c("top_left", "top_right", "bottom_left", "bottom_right")
)

mask.asymmetric.holes(k = 5, hole_size = 1)
}
\arguments{
\item{k}{Mask side length.}

\item{arm_width}{Width of the retained cross arms.}

\item{thickness}{Border thickness for \code{mask.border()}.}

\item{width}{Side length of the retained corner block for
\code{mask.corner()}.}

\item{corner}{Which corner to retain in \code{mask.corner()}.}

\item{hole_size}{Side length of each removed interior hole block for
\code{mask.asymmetric.holes()}.}
}
\value{
A logical \eqn{k \times k} keep-mask.
}
\description{
Convenience constructors for connected \eqn{k \times k} keep-masks that can
be passed to \code{\link{recursive.mask.grid.surface.graph}()} to build generalized mask
carpets. These helpers are meant to provide explicit benchmark motifs such
as cross, border, corner, and asymmetric-hole patterns.
}
\details{
The returned masks use standard matrix display orientation: rows run from top
to bottom and columns run from left to right.
}
