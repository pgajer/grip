% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grip_project3d.R
\name{grip.project.3d}
\alias{grip.project.3d}
\title{Project 3D coordinates to 2D for static plotting}
\usage{
grip.project.3d(coords, azimuth = 35, elevation = 22)
}
\arguments{
\item{coords}{Numeric matrix with at least 3 columns (only the first 3 are
used).}

\item{azimuth}{Rotation around the vertical axis in degrees. Default 35.}

\item{elevation}{Rotation around the horizontal axis in degrees. Default 22.}
}
\value{
A two-column numeric matrix of projected \code{(x, y)} coordinates.
}
\description{
Applies a rotation defined by azimuth and elevation angles and returns the
first two columns of the rotated matrix.
}
\details{
This is the projection used internally by \code{\link{grip.plot}} when
\code{projection = "ortho"} and is exported so that users can pre-project
coordinates for custom plotting.
}
\examples{
edges <- edges.torus(6, 10)
coords3d <- grip.layout(edges, n = max(edges), dim = 3,
                        preset = "torus", seed = 1)
xy <- grip.project.3d(coords3d)
plot(xy, asp = 1, pch = 16, cex = 0.5)
}
