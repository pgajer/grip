% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gisomap_layout.R
\name{grip.layout.gisomap}
\alias{grip.layout.gisomap}
\title{GISOMAP layout by graph initialization followed by edge-KK polish}
\usage{
grip.layout.gisomap(
  prepared = NULL,
  edges = NULL,
  n = NULL,
  adj_list = NULL,
  weight_list = NULL,
  edge_weights = NULL,
  dim = 2L,
  init = c("metric_mds", "weighted_grip"),
  init_args = list(),
  edge_kk_args = list(),
  diagnostics = TRUE,
  seed = 1L
)
}
\arguments{
\item{prepared}{Optional object returned by
[grip.prepare.graph.geodesic.mds()] or [grip.prepare.geodesic.kk()].}

\item{edges}{Two-column edge matrix used when `prepared` is omitted.}

\item{n}{Number of vertices used when `prepared` is omitted.}

\item{adj_list}{Optional adjacency list used when `prepared` is omitted.}

\item{weight_list}{Optional edge-weight list parallel to `adj_list`.}

\item{edge_weights}{Optional positive edge weights parallel to `edges`.}

\item{dim}{Target embedding dimension. Must be at least 2.}

\item{init}{Initializer used before edge-KK polish.}

\item{init_args}{Named list of extra arguments passed to the initializer.
For `init = "metric_mds"`, these are passed to
[grip.metric.mds.layout()]. For `init = "weighted_grip"`, these are
passed to [grip.layout.weighted()].}

\item{edge_kk_args}{Named list of extra arguments passed to
[grip.optimize.edge.kk.layout()].}

\item{diagnostics}{If `TRUE`, attach the common GMDS diagnostic panel to the
returned edge-KK-polished result and to the stored initializer metadata
when available.}

\item{seed}{Random seed passed to the weighted-GRIP initializer unless
overridden in `init_args`. Metric MDS does not use this seed.}
}
\value{
A `"grip_gisomap_layout"` object, also inheriting from
  `"grip_gmds_layout"`. The returned `coords` are the edge-KK-polished
  coordinates. The `metadata` field records the initializer, dimension,
  edge-KK method, initializer diagnostics, and edge-KK metadata.
}
\description{
`grip.layout.gisomap()` is a small orchestration wrapper for
geodesic-isometric graph embeddings. It initializes a weighted graph layout
with either metric MDS or weighted-GRIP, then polishes the coordinates with
[grip.optimize.edge.kk.layout()].
}
\details{
The name GISOMAP is used here for "geodesic-isometric mapping" rather than
classical Isomap alone. Use `init = "metric_mds"` for the Isomap-like path
and `init = "weighted_grip"` for the multiscale weighted-GRIP path.
}
