% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grip_layout_weighted.R
\name{build.weighted.misf}
\alias{build.weighted.misf}
\title{Build a weighted MISF hierarchy}
\usage{
build.weighted.misf(
  edges = NULL,
  n = NULL,
  adj_list = NULL,
  weight_list = NULL,
  edge_weights = NULL,
  num_init = 24,
  num_nbrs = 20,
  length_normalization = c("median", "mean", "none"),
  seed = 6
)
}
\arguments{
\item{edges}{Two-column integer matrix of edges (1-based vertex ids).}

\item{n}{Number of vertices.}

\item{adj_list}{Adjacency list (1-based) for undirected graphs.}

\item{weight_list}{Parallel list of strictly positive edge lengths.}

\item{edge_weights}{Optional vector of edge lengths for \code{edges}.}

\item{num_init}{Number of initial vertices in the coarsest level.}

\item{num_nbrs}{Maximum number of retained local neighbors per level.}

\item{length_normalization}{Global edge-length normalization:
\code{"median"} (default), \code{"mean"}, or \code{"none"}.}

\item{seed}{Optional RNG seed for reproducibility. If NULL, uses current time.}
}
\value{
A list with weighted MISF levels, \code{vertex_depth},
  \code{mish_order}, \code{misf_size}, \code{num_nbrs_schedule},
  \code{misf_height}, \code{top_level_size}, \code{weight_scale}, and
  \code{length_normalization}.
}
\description{
\code{build.weighted.misf()} builds the weighted max-independent-set
filtration used by the weighted GRIP layout core. It is primarily a
diagnostic and benchmarking helper for comparing weighted and combinatorial
hierarchies on the same graph.
}
