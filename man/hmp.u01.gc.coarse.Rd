% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-hmp_u01_gc_coarse.R
\docType{data}
\name{hmp.u01.gc.coarse}
\alias{hmp.u01.gc.coarse}
\title{HMP/U01 Coarsened Giant-Component Graph}
\format{
A named list with components:
\describe{
\item{adj_list}{Adjacency list of length \code{1828}, with 1-based integer
neighbor indices.}
\item{weight_list}{Parallel list of positive edge weights.}
\item{vertex_data}{Data frame with one row per coarse vertex and columns
\code{vertex_id}, \code{size}, \code{cst}, \code{subcst}, \code{dcst.depth1.absorb},
\code{dcst.depth1.rare}, \code{dcst.depth2.absorb}, \code{dcst.depth2.rare}, \code{ph}, and
\code{log10_reads}.}
\item{graph_info}{Named list summarizing provenance and graph-level
metadata, including the original and coarsened vertex counts, selected
\code{k}, edge count, and coarsening rounds.}
}
}
\source{
Derived from the publicly available HMP+U01 16S amplicon analysis
workflow used in the \code{chm_paper} project. Raw bundled artifacts and a
provenance note are available under
\verb{inst/extdata/hmp_u01_gc_coarse/}.
}
\usage{
hmp.u01.gc.coarse
}
\description{
A bundled coarsened graph derived from the giant connected component of the
HMP+U01 16S amplicon iKNN graph used in the HMP/U01 layout-selection study.
The upstream graph came from the \verb{>=1\%} relative-abundance plus PCA branch
with \code{k = 3}, after restricting to the biologically meaningful major
component. The graph was then greedily coarsened in two rounds from \code{6474}
vertices to \code{1828} supernodes.
}
\details{
The object is intended for examples and vignettes on real-world 3D layout
comparison. It is small enough to ship with the package while preserving the
arm-like graph structure that motivated the tuned preset analysis.
}
\examples{
data(hmp.u01.gc.coarse)
length(hmp.u01.gc.coarse$adj_list)
head(hmp.u01.gc.coarse$vertex_data[, c("vertex_id", "size", "cst", "subcst")])
}
\keyword{datasets}
