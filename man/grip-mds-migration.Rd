% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grip-mds-migration.R
\name{grip-mds-migration}
\alias{grip-mds-migration}
\title{Migrating classical and metric stress MDS workflows}
\description{
In grip through version 0.2.0, \code{metric.mds()} used \code{stats::cmdscale()}.
From version 0.2.0.9000, that algorithm is named \code{\link[=classical.mds]{classical.mds()}}, and
\code{\link[=metric.mds]{metric.mds()}} minimizes raw distance stress, using native SGD by default.
This is an intentional behavioral change rather than a deprecated alias.
}
\section{Preserve an existing analysis}{

Replace \code{metric.mds(...)} with \code{classical.mds(...)}, and explicit
\code{init = "metric_mds"} with \code{init = "classical_mds"}. Both \code{\link[=edge.kk]{edge.kk()}}
and \code{\link[=kernel.gram.gkk]{kernel.gram.gkk()}} default to classical initialization, preserving
their previous default algorithm without requiring smacof.
\code{add} and \code{eig} are classical-scaling arguments and are accepted only by
\code{classical.mds()}. Existing saved coordinates, figure labels, and cache
fields named \code{metric_mds} from older versions remain classical results.
Renaming a call does not require recomputing those fits.
}

\section{Request stress minimization}{

Use \code{metric.mds(...)} or \code{edge.kk(init = "metric_mds", ...)} to use the
default native SGD backend. For control over multiple starts and budgets,
call \code{metric.mds()} first and pass \code{coords = fit$coords} to \code{edge.kk()}.
Check \code{metadata$starts}, \code{metadata$termination}, and the independently
calculated stress diagnostics. Both backends are local optimizers; an
iteration limit or a finite result does not certify an optimum.
}

\section{Preserve a previous SMACOF analysis}{

Install the optional smacof package and explicitly request
\code{metric.mds(backend = "smacof", ...)}. The \code{eps} tolerance belongs to that
backend and must not be supplied to SGD. To retain a SMACOF initializer in
a refinement workflow, compute it explicitly and pass its coordinates to
\code{edge.kk()} or \code{kernel.gram.gkk()}. No automatic backend fallback is made.
}

\section{Native SGD backend}{

\code{metric.mds(backend = "sgd", max.iter = 30, ...)} explicitly selects the
default backend with a short trial budget, without requiring smacof.
SGD uses the same uniform pair weights and shortest-path targets. Its controls
remain provisional calibration choices, and \code{max.iter} counts complete pair
passes. Schedule completion is reported as \code{iteration_limit}, not convergence.
Inspect per-start records and \code{metadata$sgd} checkpoint histories. Selecting
\code{init = "metric_mds"} in refinement methods follows the default SGD backend;
their default \code{init = "classical_mds"} is unchanged. A different optimizer
cannot repair inaccurate graph distances.
}

\section{Reproducibility}{

Raw stress is measured against the original graph-distance units after
restoring scale from the backend. Target-normalized raw stress and
scale-profiled Stress-1 have the same optimal shapes when scale is free
and pair weights match, but literal Stress-1 at the returned raw-stress
scale is a different diagnostic value. Record the algorithm, objective,
backend/package versions, graph identity, scale policy, and start settings
in new result manifests. Recompute downstream refinement when changing its
initializer, and use new cache identities rather than replacing historical
classical results in place.
}

