% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grip_params.R
\name{grip.params.from.summary}
\alias{grip.params.from.summary}
\title{Extract layout parameters from a comparison summary row}
\usage{
grip.params.from.summary(summary.row)
}
\arguments{
\item{summary.row}{A one-row data frame (or the first row is used) from the
\code{$summary} element of \code{grip.compare.layouts()}.}
}
\value{
A named list with elements \code{placement}, \code{rounds},
  \code{final_rounds}, \code{num_init}, \code{num_nbrs}, \code{r},
  \code{s}, \code{repulsion_factor}, \code{tinit_factor}, and optionally
  \code{preset} (included only when the summary row records a non-empty
  preset).
}
\description{
Converts one row of the summary data frame returned by
\code{\link{grip.compare.layouts}} into a named list of parameters suitable
for passing to \code{\link{grip.layout}} or back into a
\code{grip.compare.layouts(candidates = ...)} call.
}
\examples{
edges <- edges.path(5)
cmp <- grip.compare.layouts(edges, n = 5, dim = 2,
                            candidates = "default",
                            seeds = 1L)
params <- grip.params.from.summary(cmp$summary[1, ])
coords <- do.call(
  grip.layout,
  c(list(edges = edges, n = 5, dim = 2, seed = 42L), params)
)
round(coords, 2)
}
