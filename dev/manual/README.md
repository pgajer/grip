# Manual Artifacts

This directory is the workspace for exploratory figures and PDF comparisons.

- Keep reproducible generator scripts in [`tools/`](/Users/pgajer/current_projects/grip/tools).
- Write exploratory outputs to `dev/manual/figures/`, `dev/manual/pdf/`, and `dev/manual/tmp/`.
- Do not commit generated assets here until we explicitly choose final figures.
- When an asset is finalized for user-facing docs, move that curated file into `man/figures/` and reference it from the README or vignette.
